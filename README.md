# Password-Generator

This is a Python-based password generator that continuously generates secure passwords until the user decides to stop. The tool is designed to prevent the need for repeatedly pressing the run button for each new password.
